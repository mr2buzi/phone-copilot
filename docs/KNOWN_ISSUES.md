# Known limitations

The public simulator suite passes locally without expected-failure annotations.

The remaining limitations are environmental: provider availability, Android device state, browser selector drift and live conversation variation can change runtime behaviour.

The recording demo uses its own isolated deterministic workflow. Phone execution, policy tests and the new demo checks remain outside this expected-failure list.

The deterministic demo remains the recommended way to record a repeatable walkthrough.
